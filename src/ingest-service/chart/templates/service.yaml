apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingest-service.fullname" . }}
  labels:
    {{- include "ingest-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ingest-service.selectorLabels" . | nindent 4 }}