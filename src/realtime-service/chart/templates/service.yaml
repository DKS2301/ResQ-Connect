apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-service.fullname" . }}
  labels:
    {{- include "realtime-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "realtime-service.selectorLabels" . | nindent 4 }}